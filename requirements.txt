# Pinned with floors at the tested versions and ceilings below the next major,
# so deploys are reproducible without breaking on a future major release.
streamlit>=1.40,<2
altair>=5,<7
pandas>=2,<4
numpy>=1.24,<3
scikit-learn>=1.3,<2
sentence-transformers>=2.2,<6

# NOTE: umap-learn (and its numba dependency) is intentionally NOT a runtime
# dependency. The UMAP map is served from precomputed coordinates committed under
# data/.cache/ and the manuscript point is placed numba-free, so the deployed app
# never imports umap/numba (which avoids numba's lag behind new Python releases,
# e.g. Python 3.14). Install it only to REBUILD UMAP coords offline via
# precompute.py:   pip install "umap-learn>=0.5,<0.6"
